# ===========================================
# STRIDE - Configuration
# ===========================================

# --- LLM Provider (Groq) ---
# Get your API key from https://console.groq.com/keys
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GROQ_MODEL=llama-3.1-70b-versatile
# Alternative models: llama-3.3-70b-versatile, mixtral-8x7b-32768
GROQ_WHISPER_MODEL=whisper-large-v3-turbo

# --- Opik Observability (LLM Tracing) ---
# Option 1: Opik Cloud (recommended)
# Get your API key from https://www.comet.com/opik
OPIK_API_KEY=your_opik_api_key_here
OPIK_WORKSPACE=your_workspace_name
OPIK_PROJECT=stride
# Project UUID for dashboard URLs (find in Opik dashboard URL)
OPIK_PROJECT_ID=your_project_uuid_here

# Option 2: Self-hosted Opik (uncomment if using)
# OPIK_BASE_URL=http://localhost:8085

# --- Opik Feature Flags ---
# Master switch for Opik tracing (set to 'false' to disable all tracing)
ENABLE_OPIK=true
# Realtime tracing sends spans immediately (higher latency, better debugging)
# Set to 'false' for batch mode (lower latency, recommended for production)
ENABLE_REALTIME_OPIK=false
# Log level for Opik debug output: debug | info | warn | error | none
# 'debug' = verbose span/trace logs, 'none' = suppress debug logs (production)
LOG_LEVEL=none

# --- Frontend ---
PORT=3006
VITE_APP_NAME=Stride
VITE_DEBUG=false

# Opik client-side vars for "Why this?" trace links
VITE_OPIK_WORKSPACE=your_workspace_name
VITE_OPIK_PROJECT_ID=your_project_uuid_here
# VITE_OPIK_BASE_URL=https://www.comet.com/opik  # Uncomment for self-hosted

# --- Database (DuckDB) ---
# Path to the DuckDB database file (created automatically)
DUCKDB_PATH=./data/stride.duckdb

# --- Feature Flags ---
# Enable/disable specific features
USE_GROQ_EXTRACTOR=true
EMBEDDINGS_ENABLED=true

# --- Embeddings Service (Ollama-compatible) ---
# Local development: point to local Ollama
OLLAMA_URL=http://127.0.0.1:11435
# Railway: use internal service URL
# OLLAMA_URL=http://deposium_embeddings-turbov2.railway.internal:11435
EMBEDDING_MODEL=m2v-bge-m3-1024d

# --- Google Maps API (Prospection tab) ---
# Get your API key from https://console.cloud.google.com/apis/credentials
# Required APIs: Places API, Distance Matrix API
GOOGLE_MAPS_API_KEY=AIzaSy_your_key_here

# --- SERP API (optional: job enrichment, market research) ---
# 250 free searches/month - https://serpapi.com/manage-api-key
SERP_API_KEY=your_serp_api_key_here

# --- Development ---
NODE_ENV=development
DEBUG=stride:*

# ===========================================
# Notes:
# - GROQ_API_KEY is required for LLM features (onboarding chat, analysis)
# - OPIK_API_KEY is optional but recommended for tracing LLM calls
# - If running locally without Opik, tracing will log to console
# - ENABLE_REALTIME_OPIK=false reduces latency but delays trace visibility
# ===========================================
