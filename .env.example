# ===========================================
# STRIDE - Configuration
# ===========================================
# Copy this file to .env and fill in your API keys.
# This single .env at the root is used by both frontend and MCP server.

# --- LLM Provider (OpenAI-compatible API) ---
# Stride supports ANY OpenAI-compatible provider. Just set these 3 vars:
#
# Provider examples:
#   Groq:      https://api.groq.com/openai/v1     (free tier available)
#   Mistral:   https://api.mistral.ai/v1          (free tier available)
#   OpenAI:    https://api.openai.com/v1
#   Together:  https://api.together.xyz/v1
#   OpenRouter: https://openrouter.ai/api/v1
#
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=llama-3.1-8b-instant

# Recommended models by provider:
#   Groq:    llama-3.1-8b-instant (fast), llama-3.3-70b-versatile (smart)
#   Mistral: ministral-3b-2512 (fast), mistral-large-latest (smart)
#   OpenAI:  gpt-4o-mini (fast), gpt-4o (smart)

# --- Opik Observability (optional but recommended) ---
# Get your API key from https://www.comet.com/opik
ENABLE_OPIK=true
ENABLE_REALTIME_OPIK=false
OPIK_API_KEY=your_opik_api_key_here
OPIK_WORKSPACE=your_workspace_name
OPIK_PROJECT=stride
OPIK_PROJECT_ID=your_project_uuid_here
# For self-hosted Opik, uncomment:
# OPIK_BASE_URL=http://localhost:5173

# --- Frontend (Vite) ---
VITE_API_URL=http://localhost:3001
VITE_APP_NAME=Stride
VITE_DEBUG=false
VITE_OPIK_WORKSPACE=your_workspace_name
VITE_OPIK_PROJECT_ID=your_project_uuid_here

# --- MCP Server ---
MCP_SERVER_PORT=3001
DUCKDB_PATH=./data/stride.duckdb

# --- Embeddings (optional: for semantic search) ---
# Requires deposium-embeddings-turbov2 Docker container
# See: https://github.com/theseedship/deposium_embeddings-turbov2
OLLAMA_URL=http://127.0.0.1:11436
EMBEDDING_MODEL=m2v-bge-m3-1024d

# --- Google Maps API (optional: Prospection tab) ---
# Required for job/opportunity location features
# Get your key from https://console.cloud.google.com/apis/credentials
# Required APIs: Places API, Distance Matrix API
GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# --- SERP API (optional: job enrichment) ---
# 250 free searches/month - https://serpapi.com/manage-api-key
SERP_API_KEY=your_serp_api_key_here

# --- Development ---
NODE_ENV=development
DEBUG=stride:*
LOG_LEVEL=debug

# ===========================================
# Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Get a free Groq API key: https://console.groq.com/keys
# 3. Set LLM_API_KEY with your key
# 4. Run: pnpm install && pnpm dev
# ===========================================
