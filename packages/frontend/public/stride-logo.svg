<svg width="420" height="140" viewBox="0 0 420 140"
     xmlns="http://www.w3.org/2000/svg" fill="none">

  <!-- Icon -->
  <g transform="translate(10,20)" stroke="currentColor" fill="currentColor">
    <!-- Path / stride -->
    <path d="M10 70 C40 40, 80 40, 110 20"
          stroke-width="6"
          fill="none"
          stroke-linecap="round"/>

    <!-- Arrow -->
    <path d="M105 18 L120 15 L112 28 Z"/>

    <!-- Bars -->
    <rect x="45" y="60" width="10" height="20" rx="2"/>
    <rect x="65" y="50" width="10" height="30" rx="2"/>
    <rect x="85" y="38" width="10" height="42" rx="2"/>
  </g>

  <!-- Text -->
  <text x="150" y="78"
        font-family="Inter, Helvetica, Arial, sans-serif"
        font-size="56"
        font-weight="500"
        fill="currentColor">
    Stride
  </text>

  <!-- Tagline -->
  <text x="152" y="108"
        font-family="Inter, Helvetica, Arial, sans-serif"
        font-size="14"
        letter-spacing="2"
        fill="currentColor"
        opacity="0.6">
    NAVIGATE STUDENT LIFE
  </text>
</svg>
